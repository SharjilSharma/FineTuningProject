# -- Hugging Face ecosystem --------------------------------------------------
transformers>=4.40.0
datasets>=2.19.0
peft>=0.10.0           # LoRA / QLoRA adapter training
trl>=0.8.6             # SFTTrainer, reward modelling utilities
accelerate>=0.29.0     # device-agnostic training
bitsandbytes>=0.43.0   # 4-bit / 8-bit quantisation (QLoRA)
sentencepiece>=0.2.0   # tokeniser dependency for several base models
tokenizers>=0.19.0

# -- Inference (local CPU, post fine-tune) -----------------------------------
# llama-cpp-python is installed separately with hardware flags:
#   CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python   (macOS)
#   pip install llama-cpp-python                                   (CPU only)
# Ollama is a system binary, not a pip package -- see docs/roadmap.md
# llama-cpp-python>=0.2.56   # uncomment once ready for local inference

# -- Vector store ------------------------------------------------------------
faiss-cpu>=1.8.0       # CPU-only FAISS; swap for faiss-gpu on Colab

# -- Agent orchestration -----------------------------------------------------
langgraph>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0

# -- Storage -----------------------------------------------------------------
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9  # PostgreSQL adapter
alembic>=1.13.0          # DB migrations
redis>=5.0.0             # caching layer

# -- API layer ---------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# -- Price / financial data ---------------------------------------------------
yfinance>=0.2.38
pandas>=2.2.0
numpy>=1.26.0

# -- Baseline: Loughran-McDonald lexicon --------------------------------------
# The word lists are loaded from a CSV; no special package required.
# pysentiment2 provides a convenient wrapper if preferred:
pysentiment2>=0.1.0

# -- Embeddings (for FAISS store) --------------------------------------------
sentence-transformers>=2.7.0

# -- Notebooks / exploration -------------------------------------------------
ipykernel>=6.0.0
ipywidgets>=8.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.21.0

# -- ML utilities ------------------------------------------------------------
scikit-learn>=1.4.0
scipy>=1.13.0

# -- Utilities ---------------------------------------------------------------
python-dotenv>=1.0.0   # load .env at runtime
httpx>=0.27.0          # async HTTP (API Ninjas calls)
tenacity>=8.3.0        # retry logic
groq>=1.6.0
google-genai>=0.2.2
tqdm>=4.66.0
loguru>=0.7.0
pyyaml>=6.0.1

# -- Testing -----------------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
