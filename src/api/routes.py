"""
routes.py
---------
FastAPI route definitions.

Responsibilities (Phase 7 — NOT implemented here)
--------------------------------------------------
Endpoints:
    POST /extract          -- trigger signal extraction for a transcript
    GET  /result/{id}      -- retrieve a stored extraction result
    GET  /backtest         -- return backtest correlation summary
    GET  /health           -- liveness / readiness probe
    POST /trigger/manual   -- manually push a transcript into the agent pipeline
"""
